
extends layout

block content
  - function isEmptyObject(obj) {
  -      var name;
  -      for (name in obj) {
  -        return false;
  -      }
  -      return true;
  -    }
  h1 Password Reset
  - if (isEmptyObject(errors) == false)
    p.errorMessage= "The following errors occurred:"
    ul  
      each error in errors
        li #{error.msg}
    p.errorMessage= "Please try again"

  .container
    form(method='POST', action='/accounts/password-reset/'+token)
      #form-group
        label Please enter a new password:
        input.form-control(name='password',type='password')
      br
      #form-group
        label Please re-enter your new password:
        input.form-control(name='passwordconfirm',type='password')
      br
      input.btn.btn-primary(type='submit',value='Submit')

